import { StyleSheet } from "react-native";

export const redirectLinkText = StyleSheet.create({
  textAlign: "center",
  fontSize: 16,
  color: "#1B4371",
});

export const redirectLink = StyleSheet.create({
  marginTop: 16,
});
